const server = require('./server.js');

//Index Page For API 


server.listen(5000, ()=>{
    console.log("Server is running on port: 5000")
})

//WE ARE IMPORTING ALL THE LIBRARIES / DATABASES / DEPENTANT FILES FROM THE 'SERVER.JS' FILE CONTINUE THERE ->